[project]
name = "queueiq-queuecontrol-backend"
version = "0.1.0"
description = "QueueIQ QueueControl module backend (FastAPI)"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110.0",
  "uvicorn>=0.23.0",
  "pydantic>=2.6.0",
  "pandas>=2.0.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
]
